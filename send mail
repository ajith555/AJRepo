Sub ListRules()
    Dim olApp As Outlook.Application
    Dim olSession As Outlook.NameSpace
    Dim olRules As Outlook.Rules
    Dim olRule As Outlook.Rule
    Dim condition As Outlook.RuleCondition
    Dim action As Outlook.RuleAction
    
    ' Create Outlook application and session objects
    Set olApp = New Outlook.Application
    Set olSession = olApp.GetNamespace("MAPI")
    
    ' Get the Rules collection
    Set olRules = olSession.DefaultStore.GetRules
    
    ' Check if there are any rules
    If olRules.Count = 0 Then
        MsgBox "No rules found in your Outlook account."
    Else
        ' Loop through each rule
        For Each olRule In olRules
            ' Print rule information (you can customize this part)
            Debug.Print "Rule Name: " & olRule.Name
            Debug.Print "Enabled: " & olRule.Enabled
            
            ' Display conditions
            Debug.Print "Conditions:"
            For Each condition In olRule.Conditions
                Debug.Print "    " & condition.Description
            Next condition
            
            ' Display actions
            Debug.Print "Actions:"
            For Each action In olRule.Actions
                Debug.Print "    " & action.Description
            Next action
            
            Debug.Print "==============================="
        Next olRule
    End If
    
    ' Clean up objects
    Set condition = Nothing
    Set action = Nothing
    Set olRule = Nothing
    Set olRules = Nothing
    Set olSession = Nothing
    Set olApp = Nothing
End Sub
