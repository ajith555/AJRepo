UPDATE YourTable
SET SomeColumn = 'SomeValue'
WHERE 
    (
        -- Compare the year difference
        YEAR(CAST(SUBSTRING([Latest hire date], 7, 4) AS DATE)) <= CAST('$currentYear$' AS INT) - CAST(SUBSTRING('$strHeaderFiveValue$', 2, LENGTH('$strHeaderFiveValue$') - 6) AS INT)
    )
