let
    Source = YourExchangeOnlineDataSource, // Replace with your actual data source
    FilteredRows = Table.SelectRows(Source, each [Folder Path] = "\Sent Items\" or Text.StartsWith([Folder Path], "\Inbox\")),
    // Replace 'YourExchangeOnlineDataSource' with your actual data source reference
in
    FilteredRows
